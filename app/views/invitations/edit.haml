%h2 Set your password

- semantic_form_for resource_name, resource, :url => invitation_path(resource_name), :html => { :method => :put } do |f|
  = f.error_messages
  - f.inputs do
    = f.hidden_field :invitation_token
    = f.input :password
    = f.input :password_confirmation

  = f.submit "Set my password"
