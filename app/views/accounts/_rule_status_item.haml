.item_fields
  /= bld.hidden_field :_destroy
  = bld.text_field :name
  - if rule_status.rules.size == 0
    = link_to image_tag("/images/cross.png", :border => 0, :alt => "Remove"), "#", :class => "delete_item"
  - else
    in use   
  %br
